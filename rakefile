require 'coffee-script'

task :default => 'compile_coffeescript'

task :compile_coffeescript do
	puts "Compiling Source"
	#CoffeeScript.compile File.read("script.coffee")

	puts "Compiling Jasmine Tests"
	#CoffeeScript.compile File.read("script.coffee")
end